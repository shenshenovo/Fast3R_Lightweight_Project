# @package _global_

task_name: c_task_eval
tags: ["eval", "c_task"]

# C-only controls
max_images: 100
chunk_size: 0
no_viz: true
save_metrics: true
downsample_stride: 4

data:
  data_root: /path/to/c_task_images
  num_views_val: 100

model:
  eval_use_pts3d_from_local_head: true
